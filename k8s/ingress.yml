apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: git-ingress
spec:
  
  rules:
  - host: qzvseaizzzqw.io
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: git-app-service
            port:
              number: 80