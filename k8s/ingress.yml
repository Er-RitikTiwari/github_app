apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: git-ingress
spec:
  
  rules:
  - host: 20.28.51.72
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: git-app-service
            port:
              number: 80