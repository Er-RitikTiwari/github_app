apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: git-ingress
spec:
  
  rules:
  - host: 20.28.51.72.rittikk.io
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: git-app-service
            port:
              number: 80